# Web Templates

This directory contains the HTML templates rendered by the car-price prediction application. `index.html` provides the input form and displays the predicted price returned by the server.
